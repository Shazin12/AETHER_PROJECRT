import 'package:flutter/material.dart';

const String mainCollection = "raids";
const String mainDocID = "world_boss_1";

BoxShadow defaultShadow = BoxShadow(
  color: Colors.black.withValues(alpha: 0.2),
  blurRadius: 8,
  offset: const Offset(0, 4),
);
